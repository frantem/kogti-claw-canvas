## План: Отдельные страницы /about, /faq, /careers + SEO

### 1. Новые страницы (React Router)

Создать три страницы с `react-helmet-async` для уникальных title/description/canonical/og:* и JSON-LD:

- `**src/pages/About.tsx**` (`/about`)
  - Title: «О студии красоты KOGTI в Витебске — маникюр и педикюр»
  - Контент перенесён из `AboutSection.tsx` (без потери смысла)
  - Хедер + футер сайта, кнопка «На главную»
  - JSON-LD: `AboutPage` + `BreadcrumbList`
- `**src/pages/FAQ.tsx**` (`/faq`)
  - Title: «Частые вопросы о маникюре в Витебске — KOGTI»
  - Контент перенесён из `FAQSection.tsx`
  - JSON-LD: `FAQPage` (вопросы/ответы как `Question`/`Answer`) + `BreadcrumbList`
- `**src/pages/Careers.tsx**` (`/careers`)
  - Title: «Работа мастером маникюра в Витебске — вакансии в студии KOGTI»
  - Description: «Вакансия мастера маникюра в Витебске. Студия красоты KOGTI приглашает в команду. Процент до 45%, обучение, центр города.»
  - H1: «Работа мастером маникюра в Витебске»
  - H2: «Вакансия мастера маникюра — студия KOGTI на ул. Ленина 26»
  - Главный текст пользователя — БЕЗ изменений смысла, с типографикой (списки «Что мы предлагаем» / «Кого мы ищем»)
  - 2 фото коллектива (IMG_9223.HEIC, IMG_8122.HEIC) + IMG_6971.JPG в галерее
  - Блок преимуществ (карточки в стиле AboutSection — glassmorphism)
  - CTA-блок: «Откликнуться» → ссылка/кнопка звонка `+375 33 658-26-39` + кнопка Instagram/Telegram
  - SEO-абзацы с ключами: «работа мастером маникюра Витебск», «вакансия мастера маникюра Витебск», «требуется мастер маникюра Витебск», «работа в салоне красоты Витебск»
  - JSON-LD: `JobPosting` (title, description, hiringOrganization=KOGTI, jobLocation=Витебск ул. Ленина 26, employmentType, datePosted) + `BreadcrumbList`

### 2. Обработка HEIC-фото

HEIC не отображается в браузерах. Конвертирую IMG_9223.HEIC и IMG_8122.HEIC в WebP (макс 1200px, качество ~82) и кладу в `src/assets/careers/`. IMG_6971.JPG — тоже в WebP. Использую `LazyImage`.

### 3. Главная страница (`src/pages/Index.tsx`)

- **Удалить** рендер `<AboutSection />` и `<FAQSection />` (компоненты остаются в репо как legacy, но не используются — могу удалить, если скажете).
- Это разгрузит главную и переведёт SEO-вес на отдельные URL.

### 4. Меню-гамбургер (`src/components/MobileMenu.tsx`)

Сейчас все пункты — это якоря через `scrollIntoView`. Меняю логику:

- «О нас» → `<Link to="/about">`
- «Вопросы и ответы» → `<Link to="/faq">`
- Добавляю **«Вакансии»** → `<Link to="/careers">`
- Якорные пункты («Главная», «Услуги», «Наша команда», «Контакты», «Как нас найти») остаются как есть, но при клике с не-главной страницы будут переходить на `/#services` и т.д.

Если есть десктоп-меню в `Hero`/хедере — добавлю «Вакансии» и там тоже (проверю при реализации).

### 5. Роутинг (`src/App.tsx`)

Добавить три `<Route>`: `/about`, `/faq`, `/careers`.

### 6. SEO-инфраструктура

- Установить `react-helmet-async`, обернуть `<App />` в `<HelmetProvider>` в `src/main.tsx`.
- Убрать `<link rel="canonical">` из `index.html` (каждая страница владеет своим canonical через Helmet); sitewide og:* в `index.html` оставить как fallback.
- `**public/sitemap.xml**` — добавить `/about`, `/faq`, `/careers` с `lastmod=2026-06-17`, `priority` 0.8.
- `**public/robots.txt**` — проверить, что разрешено.
- `**public/llms.txt**` — добавить ссылки на новые страницы.

### 7. Внутренние ссылки (для SEO-веса)

- В `Footer.tsx` добавить ссылки на `/about`, `/faq`, `/careers`.
- На главной в подвале блока услуг/команды добавить ненавязчивые ссылки «Подробнее о студии» → `/about`, «Часто задаваемые вопросы» → `/faq`.

### Структура

```
src/
  pages/
    About.tsx       (новая)
    FAQ.tsx         (новая)
    Careers.tsx     (новая)
  assets/careers/
    team-1.webp     (из IMG_9223.HEIC)
    team-2.webp     (из IMG_8122.HEIC)
    team-3.webp     (из IMG_6971.JPG)
```

### Уточняющие вопросы

1. **Контакт для отклика на вакансию**: использовать существующий телефон `+375 33 658-26-39` или есть отдельный номер/Telegram/Instagram для соискателей?  
Ответ: оставляй старый но так же продубллируй кнопки Telegram/Instagram он уже есть на главной теперь и на эту мтраницу добавь.
2. **Старые компоненты** `AboutSection.tsx` и `FAQSection.tsx` — удалить из репо или оставить (вдруг захотите вернуть на главную)?  
Ответ: удаляй
3. **Зарплата в JobPosting**: указать «процент до 45%» как `baseSalary` (это улучшает выдачу в Google Jobs), или оставить только текстом?
  Ответ: как `baseSalary`

&nbsp;